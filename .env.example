# Umami Cloud website ID, from the site's settings at https://cloud.umami.is
#
# Only read in production builds. Leave unset locally and the tracker is
# never emitted, so development traffic stays out of the data.
#
# For deploys, set this as a repository variable named the same way; the
# workflow passes it through to the build.
PUBLIC_UMAMI_WEBSITE_ID=
